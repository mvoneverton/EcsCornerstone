{
  "name": "ecscornerstone",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev --workspace=packages/api\" \"npm run dev --workspace=packages/web\"",
    "build": "npm run build --workspace=packages/api && npm run build --workspace=packages/web",
    "test": "npm run test --workspace=packages/api",
    "test:watch": "npm run test:watch --workspace=packages/api",
    "lint": "npm run lint --workspace=packages/api && npm run lint --workspace=packages/web",
    "db:migrate": "npm run db:migrate --workspace=packages/api",
    "db:seed": "npm run db:seed --workspace=packages/api",
    "db:seed:staging": "npm run db:seed:staging --workspace=packages/api",
    "start": "node packages/api/dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
